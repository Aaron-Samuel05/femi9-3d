export default async function Order({params}:{params:Promise<{orderNo:string}>}){const {orderNo}=await params;return <section className="section" style={{paddingTop:'18vh'}}><span className="micro">FEMI9 / ORDER</span><h1 className="display">ORDER<br/><i>{orderNo}</i></h1><p className="lead">Order tracking is represented as a polished UI in this build. Connect the production order service to populate live status.</p></section>}
