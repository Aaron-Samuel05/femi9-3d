import Link from 'next/link';
import { blogPosts } from '@/lib/data';
export default async function BlogPost({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const post=blogPosts.find(p=>p.slug===slug)||blogPosts[0];return <><section className="page-hero"><span className="micro">{post.category} · {post.date}</span><h1 className="display">{post.title}</h1></section><article className="section"><div className="legal"><p className="lead">This editorial page preserves the current Femi9 journal topic while presenting it inside the new immersive experience.</p><h2>Why this topic matters</h2><p>Period care is part of everyday life. Femi9’s current journal covers practical questions around products, periods, wellbeing, sustainability and lived experiences.</p><h2>Explore the range</h2><p>Use the product catalogue to compare sizes, flow categories, pack formats and product details before choosing a product.</p><Link className="dark-button" href="/shop">SHOP FEMI9</Link></div></article></>}
