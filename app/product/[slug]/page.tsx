'use client';
import { useParams } from 'next/navigation';
import { ArrowUpRight } from 'lucide-react';
import { products } from '@/lib/data';
import { Reveal, useCart } from '@/components/experience';
import { PadTurntable } from '@/components/pad-turntable';
export default function ProductPage(){const {slug}=useParams<{slug:string}>();const p=products.find(x=>x.slug===slug)||products[0];const add=useCart(s=>s.add);return <><section className="product-hero"><div className={`product-gallery ${p.accent}`}><img src={p.image} alt={p.name}/></div><div className="product-copy"><span className="micro">FEMI9 / PRODUCT DETAIL</span><h1>{p.name}</h1>{p.badge&&<span className="filter-chip active">{p.badge}</span>}<div className="price">₹{p.price}</div><p className="desc">{p.description}</p><div className="specs"><div><span>Flow</span>{p.flow}</div><div><span>Usage</span>{p.usage}</div><div><span>Length</span>{p.length}</div><div><span>Pack</span>{p.pack}</div></div><div className="buy-row"><button className="dark-button" onClick={()=>add(p.id)}>ADD TO BAG <ArrowUpRight/></button><button className="filter-chip">BUY NOW <ArrowUpRight/></button></div></div></section><section className="section dark product-3d-stage"><div className="grid-2"><div><span className="micro">REAL PRODUCT VIEW</span><h2 className="display">ROTATE.<br/><i>INSPECT.</i><br/>EXPLORE.</h2><p className="lead" style={{color:'#bdb2ac'}}>Interactive 360° imagery sourced from Femi9's existing product assets. Drag horizontally or scroll to move through the real product turnaround.</p></div><Reveal><PadTurntable className="product-turntable"/></Reveal></div></section><section className="section"><span className="micro">WHY FEMI9</span><h2 className="display">COMFORT.<br/><i>PROTECTION.</i><br/>CONFIDENCE.</h2></section></>}
